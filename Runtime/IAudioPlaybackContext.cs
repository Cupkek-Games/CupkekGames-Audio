namespace CupkekGames.Audio
{
    /// <summary>
    /// Marker interface for per-call audio playback context.
    /// See <see cref="AudioPlaybackContext"/> for the standard implementation
    /// (source transform for positional play, fade options for music/ambient).
    /// </summary>
    public interface IAudioPlaybackContext { }
}
